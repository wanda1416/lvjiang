# 开发日志 2026-08-05

> 接续 08-04 材料识别重构与 v0.1.3 发布。
> 本轮主题：**配置架构重构 + 调律引擎修复 + 文档体系重组 + mypy 清零 + v0.1.4/v0.1.5 发布**。

---

## 一、配置架构重构

### 1.1 配置读写统一咽喉（`8be7490`）

- `SessionStore` + `ConfigResolver` 统一咽喉；
- 封堵旁路读写。

### 1.2 顶层 config.py 收拢（`8af4d98`）

- 顶层 `config.py` 收拢至 `core/config` 包；
- 模块结构优化。

### 1.3 层根常量收拢（`e927966`）

- 常量收拢至 `resolver`；
- 封堵 `constants` 旁路。

### 1.4 移除懒加载（`766d0d4`）

- `SessionStore` 与 `ReferenceDB` 移除懒加载；
- 简化初始化流程。

### 1.5 session.py 类型窄化（`7470506`）

- `_ensure_loaded` 后 `assert _data is not None`；
- mypy 类型窄化修复。

---

## 二、调律引擎修复

### 2.1 基础规则组多套切换（`40cdb01`）

- 支持多套基础规则组切换；
- 配置架构重构。

### 2.2 装备重置后狗粮规则失效修复（`59276cd`）

- 修复装备重置后狗粮规则失效；
- 冷却期误判为次数用尽问题修正。

### 2.3 回收阻断指纹级（`2c946e9`）

- 回收阻断从格位级改为指纹级；
- 防止重复回收同一装备。

### 2.4 结束处理逻辑抽象（`914a934`）

- 结束处理逻辑抽象为公共方法；
- 消除初始判定代码重复。

### 2.5 调律场景 OCR 区域细化（`4823c44`）

- 调律场景 OCR 区域细化；
- 分组修正。

### 2.6 level_configs 迁移（`0699e1f`）

- `level_configs` 从 `tuning_base` 迁移到 `attributes.yaml`。

### 2.7 rules_editor 重命名（`b20203d`）

- `rules_editor` 目录重命名为 `tune_config`。

---

## 三、yysls 模块重构

### 3.1 模块命名与结构优化（`c473875`）

- yysls 模块命名与结构优化；
- 遵循项目规范。

### 3.2 core 模块拆分（`9565783`）

- 数据类独立建文件；
- 模块重命名。

---

## 四、文档体系重组

### 4.1 用户指南拆分（`f17a4fc`）

- 用户指南拆分为 `60-userguide/` 目录；
- 9 篇文档，共 656 行。

### 4.2 调律配置章节拆分（`c62a842`）

- 调律配置章节拆分为 5 个子文件；
- 04-01 至 04-05。

### 4.3 AI 接入文档重命名（`f48e308`）

- AI 接入文档重命名为英文文件名；
- 移除旧版 PDF 用户手册。

### 4.4 清理旧版配置文档（`ac7373e`）

- 清理已被 03-base-config + 04-* 系列替代的旧文档。

### 4.5 需求文档架构（`1054350`，08-04）

- 重构需求文档架构。

---

## 五、mypy 类型检查清零

### 5.1 批次 1-4（`17b1ea3`）

- 修复 `override`/`union-attr`/`arg-type`/`call-overload`；
- 132 → 48 errors。

### 5.2 批次 3-7（`9a7742a`）

- 修复 `arg-type`/`assignment`/`var-annotated`/`call-overload`/`misc`；
- 48 → 0 errors。

---

## 六、测试覆盖提升

### 6.1 新增 8 个测试文件（`6716ced`）

- 新增 8 个测试文件；
- 提升覆盖率 +92 用例。

### 6.2 清理死代码（`cae5ecf`）

- 清理重构过程中遗留的死代码。

---

## 七、工作流新增

### 7.1 自动购买心法（`094ab2b`）

- 新增自动购买心法工作流。

### 7.2 登录流程优化（`b7067ee`）

- 登录流程增加主页检测；
- 活动弹窗自动关闭。

---

## 八、其他改动

### 8.1 布局配置更新（`37e819d`）

- 更新布局配置、场景定义与江湖活动工作流。

### 8.2 TODO 文件迁移（`c3137ca`）

- TODO 文件移至 `docs/20-requirements/`。

---

## 九、v0.1.4 / v0.1.5 发布

### 9.1 v0.1.4（`9badfbc`）

- 版本号升级 0.1.3 → 0.1.4；
- 发布文档。

### 9.2 v0.1.5（`418c5c3`）

- 版本号升级 0.1.4 → 0.1.5；
- 发布文档。

---

## 十、结果

- 本轮提交 29 commits；
- 全部改动已提交并推送至 `origin/master`；
- mypy 0 errors；
- v0.1.4、v0.1.5 发布完成。

---

## 十一、关键设计决策

1. **配置读写统一**：`SessionStore` + `ConfigResolver` 作为唯一咽喉。
2. **回收阻断指纹级**：从格位级升级为指纹级，防止重复回收。
3. **文档体系重组**：用户指南拆分为 9 篇，调律配置拆分为 5 篇。
4. **mypy 清零**：分批次修复，最终达到 0 errors。
5. **测试覆盖提升**：新增 92 个测试用例。
