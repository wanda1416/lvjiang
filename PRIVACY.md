# 律匠隐私说明

生效版本：0.7.0 起。本文件是律匠联网行为的唯一权威说明，其余位置（首启弹窗、设置界面、README）出现的相关文案都以这里为准。

## 一分钟看完

| 律匠什么时候联网 | 发出去什么 | 能关吗 |
|---|---|---|
| 启动时查公告 | **什么都不发**，只下载一个公告文件 | 可在设置里关闭 |
| 启动时查新版本 | **什么都不发**，只问 GitHub 最新版本号是多少 | 可在设置里关闭 |
| 启动时同步在线配置（0.8.0 起新增） | **什么都不发**，只下载一份配置修复清单（若有更新） | 默认开，可在设置里关闭 |
| 匿名调律数据收集（0.7.0 起新增） | 每件装备的一次调律过程（部位/材料/词条序列/结束原因等，见下）+ 每天一次的运行环境记录 | 首次启动会问你要不要参与；开启时会补传最近 7 天尚未上报的调律数据，同意后也能随时关闭 |

前三行是这份文档最值得注意的部分：检查公告、检查更新（从第一个版本起）和同步在线配置（从 0.8.0 起）都是**只下载、零上传**——你可以在 `src/lvjiang/core/announcement.py`、`src/lvjiang/core/update.py` 和 `src/lvjiang/core/config/remote.py` 里亲自确认这一点，代码是公开的。

## 1. 律匠一共会联网做四件事

### 1.1 检查公告

启动时下载一份公告文件（`https://wanda1416.github.io/lvjiang/notices.json`），用来告诉你安全提醒、游戏更新导致的功能失效通知之类的信息。只下载，不上传任何东西。

### 1.2 检查新版本

启动时问 GitHub 最新发布的版本号是多少，用来提示你是否该升级。只下载，不上传任何东西。

### 1.3 同步在线配置（0.8.0 起新增）

启动时下载一份配置清单（`https://wanda1416.github.io/lvjiang/config/config.json`），
只在清单里的文件比你本地系统配置更新时才会拉取，用来在两次正式发布之间抢先修掉坐标偏移、
调律规则这类跑偏问题，不用等你手动更新整个程序。只下载，不上传任何东西。

**怎么生效**：下载的文件先落在暂存目录，下次启动时才会正式替换生效（不会在运行中途悄悄
换掉正在用的配置）。**你自己改过的配置始终优先，远程配置不会覆盖它**——三层配置的优先级
固定是本地 > 远程 > 系统。生效后的条目在场景编辑器、调律规则页会标一个琥珀色的**「远程」**
来源角标，和「系统」「本地」并列，点开能看到具体版本号——你随时能确认自己当前用的是不是
打了远程补丁的那份。

**怎么关**：打开「配置管理 → 网络与隐私」，取消勾选「接收在线配置更新」（默认勾选）；
勾了整体「离线模式」总闸同样会跳过它。关闭后律匠继续使用随版本发布的系统配置。

### 1.4 匿名调律数据收集（0.7.0 起新增）

**为什么要收集这个**：律匠内置的调律规则里，有不少数字目前是估的——比如"用彩狗粮在 110 阶胸甲上洗，出会心率的概率大概是多少""一件装备连着调律多少次还没出目标词条，还值不值得继续"。这些数字现在只能靠少量样本和手算，误差可能很大，而它们直接决定了内置规则会不会让你白白浪费大律准石。

每天有不少人在用律匠跑调律，每人一次会话就是几十上百次调律结果。这些结果加起来是一份任何单个玩家都攒不出来的样本。有了它，内置规则里的每一个概率和阈值都能换成实测值，让默认策略更准、"什么时候该放弃这件装备"的建议更靠谱。

**怎么问你**：新版本首次启动会弹一次窗口，说明收集什么、不收集什么，问你要不要参与。选"不同意"，律匠不会生成匿名标识，也不会发送数据；选"同意"才会开始。开启后会补传最近 7×24 小时内尚未上报的调律数据。无论选哪个，这个窗口都只弹这一次，不会每次启动都问。之后随时可以在「配置管理 → 网络与隐私」改主意。

调律历史本身是提供给你查看的本地功能，不是统计临时缓冲，因此无论是否
参与匿名统计都会保存在本地。关闭统计期间不会上传；如果之后重新开启，
只补传开启时仍在最近 7 天范围内且从未成功上报的记录，更早的数据不会上传。

## 2. 使用统计具体发出什么

### 2.1 每天一次的运行记录

| 字段 | 例子 | 为什么需要 |
|---|---|---|
| 律匠版本 | `0.7.0` | 知道还有多少人在用老版本，决定要不要发升级公告 |
| 运行环境 | `desktop` / `android` | 端游还是手游用户占比更高，决定精力往哪边投 |
| 系统 | `Windows` / `11` | 只到大版本号，决定还要不要兼容老系统 |
| CPU 架构 | `amd64` | 决定发布包要打哪几个 |
| 界面语言 | `zh_CN` | 判断值不值得继续维护英文界面 |
| 使用的插件 | `yysls` | 知道大家在用哪个游戏的插件 |
| 一个随机标识 | `8f14e45f…`（本机随机生成的一串字符） | 判断"今天打开律匠的和昨天是不是同一台电脑"，用来算多少人还在用、用了多久。你随时可以在设置里换一个新的 |

### 2.2 每件装备的一次调律过程

| 字段 | 例子 | 为什么需要 |
|---|---|---|
| 装备部位 / 武器类型 | 胸甲 / 剑 | 不同部位的词条池不同 |
| 装备等级、品阶 | 110 / 金色 | 上限值随等级变化，品阶影响词条数量 |
| 当时开启的调律规则 | `heal_fire` 这类规则标识 | 把结果和"是哪条内置规则在做决策"绑定，能直接评估规则本身的效果 |
| **调律前这件装备原有的词条** | 会心率 88.2%、破防 45% | 算"已经有会心了，再洗出会心的概率是多少"——游戏到底排不排重 |
| **逐轮记录**：加了什么材料、出了什么词条和数值、当时第几个槽位、已重置几次 | 第 2 轮 彩狗粮 → 会心率 91.5% | 顺序本身就是要统计的东西：保底、排重、材料影响什么，全靠这个序列 |
| 是否转律产出 | 是/否 | 转律的产出规律和普通调律不同 |
| **为什么结束这件装备** | 判定回收 / 判定保留 / 材料不足 / 用户停止…（固定几个选项之一） | 直接检验内置规则判得对不对——回收占比过高就说明规则太严 |
| **最终评到哪一档** | 顶级 / 优秀 / 一般 / 垃圾 | 同上，用来评估规则效果 |
| 一个随机标识 | 同上 | 用于统计上判断"这条记录和哪天的运行记录来自同一台电脑"，不含你的账号信息 |

**为什么按"一件装备"而不是"一次调律"记录**：要改进的规则问的都是序列问题——"已经有会心了，再洗出破防的概率是多少""连着调多少次还没出目标就该放弃"。逐次独立记录的数据之间没有任何关联，这些问题永远算不出来。所以记录单位是一件装备从进调律页面到离开的完整过程，包含它原有的词条。

**词条名只可能是游戏里那 37 个标准词条之一**：程序不会把识别到的原始文字直接上传。词条名必须精确命中内置的标准词条表，命中不了的——包括识别出错读到屏幕上别的文字——**整件装备的这条记录会被整条丢弃**，不是丢掉那一轮。这既是为了不让识别错误污染统计，也是为了保证不会有任何意料之外的文字被传出去。

**不收装备名、不收装备指纹**：记录里没有任何能指向"具体是你背包里哪一件"的东西。

**只报"日期"，不报具体时刻**：字段里没有任何时分秒信息，也没有单条调律记录的精确时间戳，只到天。你什么时候玩、玩了多久，律匠不会知道，也没有必要知道。

## 3. 明确不会发出什么

律匠的[问题反馈规范](docs/60-userguide/08-feedback-and-issues.md)要求你在提交 issue 前遮盖四类信息。反过来，律匠自己上传的数据里也**一条都不包含**：

- ❌ 游戏账号、角色名、任何可识别身份的信息
- ❌ ADB 设备序列号
- ❌ 本机用户名、电脑名、任何本地文件路径
- ❌ Token、Cookie、密钥

除此之外还不包含：

- ❌ 装备指纹、装备名称（词条本身会记录，见 2.2；但没有任何字段指向具体是哪一件）
- ❌ 截图、OCR 原图、日志文件、配置文件或其他未在第 2 节白名单中列明的本地内容（调律历史数据库文件本身不会上传）
- ❌ 你的 IP 地址、所在国家或地区（见下一节）
- ❌ 任何硬件序列号、MAC 地址——本机标识只是一串随机生成的字符，生成方式与你的硬件无关

技术上还有一层保证：数据库里除了那个随机标识，每一列只能存事先列好的固定几个值。服务器收到清单以外的任何内容会直接丢弃，不会存下来——这不是承诺，是代码本身的行为（见 `src/lvjiang/core/telemetry/schema.py` 与服务端的字段校验）。

## 4. 关于你的 IP 地址

和你打开任何一个网页一样，你的 IP 地址会被网络传输过程本身看到。统计服务运行在 Cloudflare 上，Cloudflare 会短暂处理它来完成这次连接、以及防止有人恶意刷接口——这和你访问任何网站时发生的事情没有区别。

律匠这边：服务器代码里不读取 IP，不出现任何 IP 相关的调用，数据库里没有"IP"这一列，也没有国家、地区、运营商这些能从 IP 推断出来的字段。判断"这是不是同一个人"这件事，由你本机生成的那个随机标识完成，不靠 IP——而那个标识你随时可以重置。

服务端代码是公开的，你可以自己翻一遍确认这一点：[`ops/stats-worker/`](ops/stats-worker/)。

## 5. 数据存在哪里、保存多久

数据存在 Cloudflare 的数据库里，只有作者一个人能访问，**不会公开发布**，也不会共享给任何第三方，只用于改进律匠内置的调律规则。

- 逐条的调律记录、逐日的运行记录保留 **90 天**，到期自动删除。
- 超过 **180 天**没有再打开过律匠的标识，会被整条删除。
- 90 天之后只留下不含任何标识的汇总数字（比如"8 月 1 日有多少人打开律匠"这类计数），这些数字已经和任何人对不上了，因此可以无限期保留。

表结构与查询方式（面向想了解技术细节的读者）见 [数据库设计文档](docs/30-architecture/06-telemetry.md)。

## 6. 谁能看到这些数据

只有作者本人。这些数据不出售、不共享给任何第三方、不接入任何第三方统计或分析服务。

## 7. 我要怎么关掉 / 重置标识 / 删掉已经发出去的数据

**不想再参与了**：打开「配置管理 → 网络与隐私」，取消勾选「参与匿名数据收集」。关闭期间不会上传，并会删除匿名标识和临时发送缓冲；供你查看的本地调律历史不会删除。如果之后重新开启，会用新的匿名标识补传最近 7 天内尚未上报的调律记录。

**想换一个新标识**：同一页点「重置标识」。律匠会生成一个全新的随机标识，从此之后的记录和之前的对不上。

**想删掉已经上传的记录**：在设置里复制你的标识（前几位就够用），发到 issue 或交流群里找作者，会删除数据库里所有带这个标识的记录。

两个需要你知道的限制：

1. **请先申请删除，再重置标识**——一旦重置，旧标识就找不回来了，作者也没有办法再定位到你之前的数据。
2. 已经被合并进统计汇总的部分**无法单独撤回**——汇总里只剩"某个组合出现了多少次"这样的纯计数，不含任何标识，技术上已经无法追溯到是谁贡献的，这也是它可以长期保留的原因。

你不需要联系作者证明"这是我的标识"，不需要在公开场合贴出任何能确认你身份的信息——那串随机字符本身就是全部所需。

## 8. 这份说明会变吗

如果收集的内容范围有实质性变化，律匠会：

1. 先更新这份文档；
2. 通过发布说明与内置的公告通道提前告知；
3. 才会让新版本正式生效。

不会在没有说明的情况下，悄悄扩大收集范围。

## 9. 有疑问找谁

微信交流群（主窗口「帮助 → 反馈」扫码加群）或 [GitHub Issue](https://github.com/wanda1416/lvjiang/issues)，参见[反馈规范](docs/60-userguide/08-feedback-and-issues.md)。
