# Dev Log: 2026-07-20 属性规则统一、Panel 类型系统与 grid 校准重构

> 日期：2026-07-20
> 涉及模块：`lvjiang/core/ocr.py`、`lvjiang/core/android/`、`lvjiang/equip_parser/`、`lvjiang/workflows/`、`lvjiang/ui/`、`config/system/`、`tests/`
> 关键词：OCR 预处理移除、属性规则统一管理、cap_pct 数值百分比、词条映射配置化、Panel 类型系统、DSL 范围迭代、grid 校准亮度二值化

> 说明：本篇为事后补写（原 07-17~20 无每日日志，仅 07-21 演进总结覆盖），依据当日 git 提交还原。

---

## 一、本日主线

当日 9 个提交，三条主线：**OCR/解析链路修正**（预处理移除 + 装备解析纠错）、**属性与词条配置化**（属性规则统一 + 词条映射配置化）、**DSL 与识别能力增强**（Panel 类型系统 + 范围迭代 + grid 校准重构）。

---

## 二、OCR 与装备解析链路修正

### 2.1 OCR 预处理优化与截图颜色修复（`b524f7a`）

OCR 预处理优化，修复截图颜色问题。

### 2.2 OCR 预处理移除 + 装备解析纠错（`67b4117`，6 files +316/-67）

- 实测后**移除 OCR 预处理**（预处理对识别率收益不明显，反增复杂度）；
- 装备解析纠错；场景配置更新。

### 2.3 scrcpy 连接首次失败修复（`bc09273`）

修复 scrcpy 首次连接失败问题（重试/握手时序）。

---

## 三、属性与词条配置化

### 3.1 属性规则统一管理（`5cef3fb`，15 files +1122/-183）

属性规则从散落各处收敛为统一管理，为后续 attributes.yaml 单一权威源打基础。

### 3.2 cap_pct 数值百分比 + 输出按用户归档（`180bbfe`）

- 装备词条增加 `cap_pct`（数值百分比上限）；
- 工作流输出按用户归档（多用户隔离）。

### 3.3 词条映射配置化 + 属性管理 UI 增强（`2e190e2`，5 files +956/-311）

词条映射从硬编码改为配置化；属性管理 UI 增强。

---

## 四、DSL 与识别能力增强

### 4.1 Panel 类型系统 + DSL 范围迭代语法（`404d34f`，32 files +2186/-244）

- 新增 **Panel 类型系统**（面板作为一类独立的场景对象，支持子区域布局）；
- DSL 新增范围迭代语法（`for i in range(...)`）。

### 4.2 grid 校准算法重构（`3611dd2`，16 files +683/-174）

grid 校准算法重构为**亮度二值化 + 短区间过滤**，提升格子定位鲁棒性（背包/材料网格识别更稳）。

### 4.3 PostMessage drag 命中测试（`deaf280`）

PostMessage 拖拽补发 `WM_NCHITTEST` 命中测试，修复后台拖拽落点问题。
