# 开发日志 2026-07-31

> 接续 07-30 Android Phase 3 完成与 DSL P2 能力补充。
> 本轮主题：**DSL 文档重组 + 调律规则模型大重构 + 平台适配层抽离 +
> 打包与质量门禁 + macOS 初步支持**。

---

## 一、DSL 语法文档重组（`1d31a5e`）

### 1.1 背景

原有 DSL 文档结构混乱，概念与语法混杂，交叉引用困难。

### 1.2 改动

- 重写 `01-basics.md`：词法与值/引用模型/变量/表达式四节；
- 新增 `02-concepts.md`：场景/布局/Area/Action/Panel 精简；
- `03-interaction.md`：概念上移到 02，只留指令语法；
- 重写 `04-data-flow.md` → `01`，eval 简化；
- 文件重命名：04→05-control-flow, 05→06-functions, 06→07-subworkflows, 07→08-examples；
- 删除旧 `02-data-flow.md`，全部交叉引用同步更新。

---

## 二、调律规则模型大重构

### 2.1 行为状态机配置重构（`65d6cd3`）

- `behavior` 段替代 `recycle` 段；
- 调律行为（保留/重置/跳过）配置化。

### 2.2 判定语义下沉（`282809f`）

- 判定逻辑下沉到逐条处置规则；
- 无适用规则时兜底为垃圾档。

### 2.3 规则模型扩展（`5dcd0fa`, `934fbab`, `54fb43e`）

- 顶档/普通条件结构演进；
- `contains_all` 组合条件与首词条约束收紧；
- 仅首词条逐规则化 + 首词条方向比较；
- 潜力日志改进。

### 2.4 势语义落到规则配置（`2d8f5a6`）

- 属攻命名对齐具体流派；
- 治疗火拳顶级条件属攻改用真实名（`036414a`）。

### 2.5 规则编辑器改进（`da22099`, `5e8de54`, `ceb447f`）

- 判定区候选收窄为可用词条库；
- 词条选择支持平铺形态；
- 可用词条库填满剩余高度，已选词条行内 ✕ 快速移除；
- 调律门槛迁基础配置页 + 行为页仅首词条列 + 布局优化。

---

## 三、平台适配层抽离（`309bb04`）

### 3.1 背景

平台差异（Windows/macOS/Android）此前散落在各处，维护困难。

### 3.2 改动

- 抽离 `core/platforms.py` 统一收口主流程平台差异；
- 输入模拟配置重构 `DelayConfig` → `InputSimConfig`（`a6e2317`）。

---

## 四、macOS 初步支持（`2e72b30`, `9f09d8f`）

### 4.1 Phase 0 依赖验证

- macOS 依赖验证通过；
- 退出崩溃修复（use-after-free + pynput libffi 递归）。

### 4.2 macOS 平台支持计划（`01c58f9`）

- ADB 模式优先，投屏与视觉识别后置。

---

## 五、打包与质量门禁

### 5.1 ruff + mypy 门禁（`b5d1de0`）

- 接入 ruff + mypy 门禁；
- 修掉全仓存量告警。

### 5.2 PyInstaller 一键打包（`d415aa6`）

- onedir 模式打包（launcher + spec + package.bat）；
- 内置 adb 随包分发，用户免装 platform-tools（`fd45daf`）。

### 5.3 Android 构建优化（`7f2731f`）

- release 开启 R8 与可选签名；
- sync 任务改挂 preBuild。

---

## 六、Android 设备端完善

### 6.1 悬浮服务任务入口（`da9a6e9`）

- 悬浮服务作为任务入口；
- 补无障碍掉线引导。

### 6.2 首次启动引导流程（`bc4b2f8`）

- e2e 回归补齐 R8 反射面。

### 6.3 正式签名落地（`b6b8b02`）

- keystore 生成 + README-signing.md 说明文档。

### 6.4 FloatService 输入注入（`f15c588`）

- 输入注入与构建配置更新。

### 6.5 原生调律参数配置页（`5d51117`）

- 部位会话回退修复。

---

## 七、工作流引擎增强

### 7.1 整面板扫描与数字 key 访问（`9a0c9ef`）

- 单格结果统一为 key 过滤语义；
- `[r][c]` 数字 key 访问。

### 7.2 匹配置信度阈值调整（`38edea2`）

- 默认阈值 0.05 → 0.35；
- 依据日志实测分布（n=893，0.46~0.67）。

### 7.3 静态预检（`219e7c0`）

- 引擎新增 `validate_only` 静态预检；
- 系统工作流引用门禁与上机预检脚本。

### 7.4 工作流修复

- 定位失败不再静默跳过，改为抛错中断（`6361817`）；
- 未绑定的区域不再被当成未命中（`8423de9`）；
- 跑脚本前校验静态引用是否已在布局绑定（`2cbbb75`）；
- 江湖号令返回按钮 key 修复（`46b833c`）。

---

## 八、场景编辑器改进

### 8.1 视图新建合并（`3714457`）

- 视图新建合并为单对话框；
- 区域/视图 key 重复实时红字提示并禁用 OK。

### 8.2 视图过滤重构（`a050127`）

- 视图过滤放弃基底叠加，基底降级为普通视图。

### 8.3 场景分组重排（`ee290d2`）

- 场景分组重排与改名；
- 新增情境场景及号令情境视图区域。

---

## 九、配置架构

### 9.1 双模式三层配置（`16c20bc`）

- `config_resolver` 统一解析；
- 默认布局与参考图入库为系统资产。

### 9.2 布局自动回退（`33c1baf`）

- `active_layout` 未指定时自动回退到第一个可用布局。

---

## 十、调律工作流

### 10.1 UI 注入配置收口（`bb6071f`）

- 调律工作流的 UI 注入配置收口为 `TuningRunContext`。

### 10.2 遍历回收修复（`694e409`）

- 遍历回收修复 + 等级门槛 + F10 保留现场。

### 10.3 重置调律语义修正（`9efc158`）

- 只剩首词条、冷却期硬限单件一次；
- 二次确认前等 6-7s。

### 10.4 非首外功攻击硬门槛（`1eb5f3f`）

- 缺大外/小外能用封顶。

---

## 十一、江湖号令工作流（`b2523fb`, `f6bf834`, `2cb181a`）

- 补齐情境任务动作流程；
- 门派页无门无派分流与合影退出路径修正；
- 东方第一枝（看报）落地；
- 心得场景改名 `xinde_exchange` → `training_xinde`。

---

## 十二、装备解析器修复（`9cbcf39`）

- 定音词条改全量池匹配，不再依赖装备部位推断。

---

## 十三、应用稳定性

- 退出时先停 loguru 异步写入线程，避免退出挂起（`20acde0`）；
- `_wait_for_threads` 排除 `_DummyThread` 避免 join AssertionError（`feb7518`）。

---

## 十四、文档

- 新增用户指南（`efaa41e`）；
- 调律说明文档与规则语义同步（`da7296c`）；
- 更新 todo-android 进度笔记（`d802095`）。

---

## 十五、结果

- 本轮提交 60+ commits；
- 全部改动已提交并推送至 `origin/master`。

---

## 十六、关键设计决策（用户确认）

1. **DSL 文档重组**：概念与语法分离，编号顺延。
2. **行为状态机配置**：`behavior` 段替代 `recycle` 段。
3. **平台适配层**：`core/platforms.py` 统一收口。
4. **双模式三层配置**：系统/会话/本地，config_resolver 统一解析。
5. **匹配置信度阈值**：0.05 → 0.35，基于实测分布。
6. **重置调律语义**：冷却期硬限单件一次。
