# TODO

## 调律规则后续待办

### 待办

1. **交互互通**：「游戏配置」与「调律规则」对话框右上角互挂入口按钮
   （属性 ↔ 规则 ↔ 验证 成环）；进阶：属性管理重命名词条时扫描调律
   规则引用并提示批量替换。
2. **weapons 表引用流派配置**：调律规则各 YAML 的 weapons 表仍各自
   硬编码（旧式 `{main: {武器: 词条}, sub: [武器]}`），未来改为引用
   attributes.yaml 的 `schools` 流派配置；新增流程收敛为：装备配置加武器 →
   指定武学增效加词条 → 流派配置绑定，调律规则零改动。
