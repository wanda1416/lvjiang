# 开发日志 2026-07-24

> 接续 07-23 装备解析与 UI 布局优化。
> 本轮主题：**单包插件化架构重构 + 参考图管理 + 文档结构重构**。

---

## 一、单包插件化架构重构（`b2b6361`）

### 1.1 背景

原有架构不利于插件扩展，需要重构为单包插件化架构。

### 1.2 改动

- 重构为单包插件化架构；
- 便于后续插件扩展。

---

## 二、移除 EQUIP_REGIONS 硬编码（`040df7a`）

- 移除 `EQUIP_REGIONS` 硬编码；
- canvas 初始化为空列表。

---

## 三、修复 material_recognizer 导入路径（`0ba6816`）

- 修复迁移至 `src.apps.yysls.core` 后遗漏的导入路径。

---

## 四、文档结构重构与编号规范化（`20042fe`）

- 文档结构重构；
- 编号规范化。

---

## 五、架构文档归集（`79c8211`）

- 架构文档归集至 `30-architecture`；
- 链接修复。

---

## 六、图像参考库通用化（`1a4696f`）

- 图像参考库通用化；
- 参考图管理 UI 增强。

---

## 七、结果

- 本轮提交 6 commits；
- 全部改动已提交并推送至 `origin/master`。

---

## 八、关键设计决策（用户确认）

1. **单包插件化架构**：便于后续插件扩展。
2. **参考库通用化**：图像参考库不再绑定特定业务。
