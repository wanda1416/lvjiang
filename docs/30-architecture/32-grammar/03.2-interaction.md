# DSL 时间与辅助指令

> 交互指令概览见 [03-interaction.md](03-interaction.md)。鼠标操作（click/drag）见 [03.3-mouse.md](03.3-mouse.md)。键盘输入（press）见 [03.4-keyboard.md](03.4-keyboard.md)。
>
> 操作对象（Entity / CoordRef / Panel）的概念说明见 [02-concepts.md](02-concepts.md)。

## 目录

- [一、wait — 等待](#一wait--等待)
- [二、align — 面板自对齐](#二align--面板自对齐)
- [三、screenshot — 截图](#三screenshot--截图)
- [四、失败语义](#四失败语义)

## 一、wait — 等待

暂停执行。两种模式：**定时等待**（固定/随机时长）和**稳定等待**（画面不再变化时继续）。

### 1.1 定时等待

支持命名延迟、固定秒数、动态变量和随机范围四种形式。

**语法**：

```
wait @<delay_name>      # 命名延迟（从延迟配置中读取，@ 前缀必须）
wait <秒数>             # 固定等待
wait $var               # 动态等待（变量值为秒数）
wait (min, max)         # 随机范围等待（在 min~max 秒之间随机取值）
```

**示例**：

```
wait @page_refresh     # 命名延迟（从配置读取）
wait 1.5                    # 固定等待 1.5 秒
wait $interval              # 动态等待，$interval 的值是秒数
wait (1, 2)                 # 随机等待 1~2 秒
```

**说明**：

- `$var` 的值可以是数字（固定等待）或元组 `(min, max)`（随机范围等待）
- 配合 `eval $var = (min, max)` 或 `default $var = (min, max)` 赋值后使用
- `wait @<delay_name>` 引用的命名延迟必须已在「配置管理 → 等待参数」中定义，否则报错终止（详见[失败语义](#四失败语义)）
- 命名延迟必须使用 `@` 前缀，裸标识符（如 `wait page_refresh`）是语法错误

**语法形式对照**：

| 语法 | 支持 | 说明 |
|------|------|------|
| `wait 1.5` | ✅ | 固定数值 |
| `wait (1.0, 3.0)` | ✅ | 数值范围（字面量） |
| `wait $var` | ✅ | 变量（值可以是 number 或 tuple） |
| `wait ($a, $b)` | ✅ | 混合引用（变量 + 数字任意组合） |

`wait ($a, $b)` 中每个元素可以是数字或变量引用，运行时解析后在 `lo~hi` 范围内随机取值。配合 `eval $var = ($a, $b)` 可将元组存入变量后 `wait $var` 使用。

### 1.2 稳定等待（wait stable）

连续截图对比，当画面在指定时长内没有明显变化时，认为「加载完成 / 动画结束」，继续执行下一步。适用于加载时间不确定的场景，替代固定延迟等待。

**语法**：

```
wait stable <timeout>                                                      # 基本形式
wait stable <timeout> on [scene].[region]                                  # 只检查指定区域
wait stable <timeout> threshold <value>                                    # 自定义差异阈值
wait stable <timeout> interval <value>                                     # 自定义截图间隔
wait stable <timeout> duration <value>                                     # 自定义稳定持续时长
wait stable <timeout> least <value>                                        # 自定义最低等待时间
wait stable <timeout> on [scene].[region] threshold <v> interval <v> duration <v> least <v>
```

完整语法为：

```text
wait stable <timeout> [on <scene>.<region>]
    [threshold <value>] [interval <value>] [duration <value>] [least <value>]
```

`on` 如果存在，必须紧跟在 `timeout` 后、各数值选项之前；数值选项之间可以任意排序。

**参数值形式**：

所有参数（timeout / threshold / interval / duration / least）均支持三种形式：

| 形式 | 示例 | 说明 |
|------|------|------|
| 字面量数字 | `wait stable 5` | 直接指定秒数 |
| `@命名延迟` | `wait stable @page_load` | 从延迟配置中读取（取范围中值） |
| `$变量引用` | `wait stable $my_timeout` | 运行时从变量读取 |

**参数**：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `timeout` | （必填） | 等待预算秒数；预算内未稳定则记警告并继续执行（不报错） |
| `threshold` | 0.02 | 像素差异率阈值（0~1），低于此值认为「画面没变」 |
| `interval` | 0.3 | 截图对比间隔秒数 |
| `duration` | 0.5 | 画面需持续稳定的时长（秒），连续差异低于阈值达到此时长后返回 |
| `least` | 0.5 | 最低等待秒数，点击后至少等这么久再开始检测稳定，防止转场动画未开始就被误判为「画面已稳定」 |
| `on` | 全画面 | 将差异计算限定到当前布局中绑定的一个区域；写法为 `[scene].[region]` |

#### 区域限定（on）

默认情况下，`wait stable` 比较整张截图。游戏主画面持续播放角色动作、粒子或背景动画时，即使关心的按钮或弹窗已经稳定，整图差异仍可能一直超过阈值。此时可用 `on` 只比较真正决定下一步能否执行的 UI 区域：

```wf
# 只观察背包装备列表；列表稳定即可继续，不受旁边角色动画影响
wait stable 8 on [equip_page].[bag_area]

# 区域限定可与其他参数同时使用
wait stable @page_refresh on [general_control].[confirm] least 0.8 duration 0.6

# 操作后的内联等待同样支持 on
click [equip_page].[sort] after wait stable 5 on [equip_page].[bag_area]
```

执行时，引擎从**当前布局**读取 `[scene].[region]` 的归一化区域，将其与布局画布进行二次坐标换算，得到截图像素裁剪框。每轮截图先裁剪到该矩形，再与上一帧计算差异；`threshold` 的含义和全画面模式完全一致。

区域引用支持静态名称和变量：

```wf
eval $watch_scene = "equip_page"
eval $watch_region = "bag_area"
wait stable 8 on $watch_scene.$watch_region

# 也可以只让其中一段动态化
wait stable 8 on [equip_page].$watch_region
```

静态引用会在工作流预检阶段确认场景和区域已在当前布局中绑定；动态引用在运行时解析。变量未定义、场景不存在、区域未绑定或区域被禁用时，都会作为工作流错误报告，而不是静默改为全画面检测。

`on` 当前只接受普通场景区域（Region），不接受坐标点、方向、网格及网格单元，也不接受 `[scene].[subscene].[entity]` 三段子场景引用。需要观察子场景内容时，应在父场景中定义一个覆盖目标范围的普通区域，再将该区域用于 `on`。

> `after wait stable` 不会自动沿用前面操作的目标。例如 `click [scene].[button] after wait stable 5` 仍然检查全画面；必须显式写成 `after wait stable 5 on [scene].[panel_area]` 才会限定区域。

**示例**：

```
wait stable 5                    # 等待画面稳定，最多 5 秒
wait stable 5 on [equip_page].[bag_area]  # 只检查背包区域
wait stable 3 threshold 0.01     # 严格模式，差异 < 1% 视为稳定
wait stable 10 interval 0.5      # 每 0.5 秒检测一次，最多等 10 秒
wait stable 5 duration 1.0       # 画面需连续稳定 1 秒才算完成
wait stable 5 least 1.0          # 点击后至少等 1 秒再开始检测（慢加载页面）
wait stable 10 threshold 0.03 interval 0.5 duration 1.0 least 0.3  # 完整参数

# 也可作为 click/drag 的后缀等待子句（before/after/around 均可）
click [activity_jianghu].[btn] after wait stable 8 least 0.5
```

**说明**：

- 工作原理：每 `interval` 秒截图一次，与上一帧计算像素差异率（`cv2.absdiff.mean() / 255`），连续差异低于 `threshold` 的时长达到 `duration` 后返回
- 指定 `on` 时，每一帧都会先裁剪到当前布局绑定的区域，再计算差异；未指定时比较全画面
- `timeout` 是等待预算而非硬性断言：预算耗尽仍未稳定时记录警告并继续执行。游戏画面常有持续动画，永远达不到阈值是常态；实际书写时建议把 `timeout` 设得宽裕些（如 8~10），至少不会比固定等待差
- `least` 期间只截图建立基准，不进行稳定判定。防止点击后转场动画尚未开始，画面恰好「没变」就被误判为稳定
- 等待期间持续检查停止标志，F10 / 停止按钮可立即中断
- 可作为 click/drag 的后缀等待子句：`click ... after wait stable <timeout> ...`，与普通 wait 子句展开规则一致（around 前后各执行一次）
- 所有参数支持字面量数字、`@命名延迟`、`$变量引用` 三种形式
- `threshold`、`interval`、`duration` 和 `least` 可以任意顺序书写
- `on` 不是数值选项，必须写在 `timeout` 与其他选项之间

## 二、align — 面板自对齐

对 Panel 进行图像自对齐，计算实际网格间距并缓存格子中心坐标。

**语义**：`align` 是 `align_func(scene, panel)` 的语法糖。首次对 Panel 执行 `click [scene].[panel][r][c]` 时自动触发，无需手动调用。

**语法**：

```
align [scene].[panel]         # 对指定 Panel 执行图像自对齐
```

**示例**：

```
# 手动触发对齐（通常不需要，click 会自动触发）
align [bag_equip_detail].[bag_grid]

# 后续点击直接使用缓存坐标
click [bag_equip_detail].[bag_grid][1][1]     # 不再触发对齐
click [bag_equip_detail].[bag_grid][2][3]     # 直接查缓存
```

**说明**：

- 对齐结果缓存在 `_alignment_cache` 中，同一 Panel 只需对齐一次
- 窗口缩放、分辨率变化后需要重新对齐（引擎会自动检测）
- 对齐算法基于方差分析，自动检测网格间距
- 详见 [02-concepts.md — 自对齐机制](02-concepts.md#自对齐机制)

## 三、screenshot — 截图

截取当前画面并保存到 `logs/image/` 目录，用于调试和记录。

**语法**：

```
screenshot
```

**示例**：

```
# 在关键步骤后截图，便于调试
scan [equip_detail].[affixes] as $affixes
screenshot                    # 保存当前画面
click [equip_detail].[confirm]
```

**说明**：

- 文件命名格式：`image_YYYYMMDD_HHMMSS_mmm.png`（精确到毫秒）
- 保存目录：`logs/image/`（自动创建）
- 日志输出保存的文件名，便于事后查找
- 截图失败时仅记录警告，不中断执行

## 四、失败语义

时间与辅助指令的失败按原因分两类：

**配置错误 → 抛错终止**：

| 情况 | 指令 |
|------|------|
| Panel 未在布局中定义 | align |
| `wait @<delay_name>` 的命名延迟未定义 | wait |
| 拿不到截屏尺寸（截屏后端不可用） | 任何需要换算坐标的指令 |

**运行时状态 → 记日志后跳过**：

- Panel 对齐失败（页面未加载完、列表为空，`detect_grid` 检测不到 slot）

抛出的错误可以被 `try` / `catch` 捕获，需要容错的片段自行包裹即可。

配置错误在**执行前**就会被静态检查拦下：引擎解析完 `.wf` 先把全部静态引用与当前布局比一遍，一次性列出所有问题（含文件名与行号），不等执行到那一行。详见 [33-engine/02-static-check.md](../33-engine/02-static-check.md)。
