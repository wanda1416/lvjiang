# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  装备基础属性规则
#  用途：根据基础属性数值判断装备品阶、校验 OCR 解析正确性
#  原则：仅存储精确匹配数据，不做比例推算，逐步补充
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ── 武器：外功攻击（范围 [min, max]） ──
# 适用于 main_weapon / sub_weapon
weapon:
  110:
    gold:   { min: 100, max: 232 }
    purple: { min: 90,  max: 209 }
    blue:   { min: 80,  max: 186 }
  105:
    gold:   { min: 87, max: 203 }
    purple: { min: 79,  max: 183 }

# ── 戒指：最小外功攻击（单值） ──
ring:
  110:
    gold: 133
    purple: 120
    blue: 106

# ── 佩饰：最大外功攻击（单值） ──
pendant:
  110:
    gold: 199
    purple: 179
    blue: 159

# ── 防具（非胸甲）：气血最大值（单值） ──
# 适用于 head / leg / wrist，同等级同品阶值相同
armor_other:
  110:
    gold: 9723
    purple: 8750
    blue: 7778

# ── 胸甲：气血最大值（单值） ──
chest:
  110:
    gold: 19445
    purple: 17500
    blue: 15556
    
