#% name: 从背包返回主界面

# 从装备详情弹窗返回主界面
# 路径：详情弹窗 → 背包页 → 菜单页 → 主界面
# 前置：在背包页且装备详情弹窗已打开（bag_equip_detail 场景内）
# 后置：在主界面
# 注意：详情弹窗是覆盖在背包页上的隐形弹窗，需先关闭再逐层返回

def nav_back_to_main()
    # 关闭详情弹窗 → 背包页
    click [bag_equip_detail].[back] after wait page_refresh_wait
    # 背包页 → 菜单页
    click [bag_equip_detail].[back] after wait page_refresh_wait
    # 菜单页 → 主界面
    click [game_menu_page].[back] after wait page_refresh_wait
end
